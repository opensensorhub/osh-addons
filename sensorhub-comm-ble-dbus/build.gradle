description = 'Bluetooth LE Comm Network (D-Bus)'
ext.details = 'Bluetooth Smart (Low Energy) network adapter based on D-Bus (Linux only)'

dependencies {
  compile project(':sensorhub-core')
  compile project(':sensorhub-comm-ble')
  embedded files('libs/org/freedesktop/dbus/2.8/dbus-2.8.jar')
}
