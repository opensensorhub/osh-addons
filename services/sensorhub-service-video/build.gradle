description = 'SOS Extensions for Video'
ext.details = 'Sensor Observation Service extensions for supporting video'
version = '1.3.0'


dependencies {
  implementation 'org.sensorhub:sensorhub-core:' + oshCoreVersion
  implementation 'org.sensorhub:sensorhub-service-swe:' + oshCoreVersion
  implementation 'org.sensorhub:sensorhub-service-sweapi:' + oshCoreVersion
  
  // mp4parser has a transitive dependency on a beta version of slf4j-api, so we explicitly exclude its
  // dependency and include a non-beta one.
  implementation('org.mp4parser:isoparser:1.9.56') { exclude group: 'org.slf4j', module: 'slf4j-api' }
  implementation('org.mp4parser:muxer:1.9.56')     { exclude group: 'org.slf4j', module: 'slf4j-api' }
  implementation('org.mp4parser:streaming:1.9.56') { exclude group: 'org.slf4j', module: 'slf4j-api' }
  implementation 'org.slf4j:slf4j-api:1.7.36'
}

// add info to OSGi manifest
osgi {
  manifest {
    attributes('Bundle-Vendor': 'Sensia Software LLC')
    attributes('Bundle-Activator': 'org.sensorhub.impl.service.sos.video.Activator')
  }
}

// add info to maven pom
ext.pom >>= {
  developers {
    developer {
      id 'alexrobin'
      name 'Alex Robin'
      organization 'Sensia Software LLC'
      organizationUrl 'http://www.sensiasoftware.com' 
    }
  }
}

