description = 'SOS Extensions for Video'
ext.details = 'Sensor Observation Service extensions for supporting video'
version = '1.2.0'


dependencies {
  implementation 'org.sensorhub:sensorhub-core:' + oshCoreVersion
  implementation 'org.sensorhub:sensorhub-service-swe:' + oshCoreVersion
}

// add info to OSGi manifest
osgi {
  manifest {
    attributes('Bundle-Vendor': 'Sensia Software LLC')
    attributes('Bundle-Activator': 'org.sensorhub.impl.service.sos.video.Activator')
    attributes('-exportcontents': 'org.sensorhub.impl.service.sos.video')
    attributes('Import-Package': '''
      !javax.crypto.*,!javax.imageio.*,!javax.xml.*,!java.*, !org.w3c.*, !org.xml.*,
      org.osgi.service.repository;resolution:=optional,
      *''')
  }
}

// add info to maven pom
ext.pom >>= {
  developers {
    developer {
      id 'alexrobin'
      name 'Alex Robin'
      organization 'Sensia Software LLC'
      organizationUrl 'http://www.sensiasoftware.com' 
    }
  }
}

