description = 'Smart Camera Sensor'
ext.details = "Driver for Smart Cameras"
version = '1.0.0'

dependencies {
    implementation 'org.sensorhub:sensorhub-core:' + oshCoreVersion
    implementation project(':sensorhub-driver-videocam')
    implementation project(':sensorhub-rpi-common')
    implementation 'org.bytedeco:javacv-platform:1.5.3'
    testImplementation('junit:junit:4.13')
}

// exclude tests requiring connection to the sensor
// these have to be run manually
//      If tests are to be excluded list them here as follows
//          exclude '**/TestNameClass.class'
test {
    useJUnit()
}

// add info to OSGi manifest
osgi {
    manifest {
        attributes('Bundle-Vendor': 'Sentinel, LLC')
        attributes('Bundle-Activator': 'org.sensorhub.impl.sensor.video.smartcam.Activator')
    }
}

// add info to maven pom
ext.pom >>= {
    developers {
        developer {
            id 'nickgaray'
            name 'Nick Garay'
            organization ''
            organizationUrl ''
        }
    }
}