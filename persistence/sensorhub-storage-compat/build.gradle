description = 'OSH v1 Storage Compatibility Package'
ext.details = 'Adaptater package to use OSH v1 storage files with OSH v2 datastore API'
version = oshCoreVersion

dependencies {
  implementation 'org.sensorhub:sensorhub-core:' + oshCoreVersion
  testCompile group:'org.sensorhub', name:'sensorhub-core', version:oshCoreVersion, configuration: 'testArtifacts'
}


// add info to OSGi manifest
osgi {
  manifest {
    attributes('Bundle-Vendor': 'Sensia Software LLC')
    attributes('Bundle-Activator': 'org.sensorhub.impl.persistence.Activator')
  }
}

// add info to maven pom
ext.pom >>= {
  developers {
    developer {
      id 'alexrobin'
      name 'Alex Robin'
      organization 'Sensia Software LLC'
      organizationUrl 'http://www.sensiasoftware.com' 
    }
  }
}

